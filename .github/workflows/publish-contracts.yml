name: Publish @globiguard/contracts

permissions:
  contents: read
  id-token: write

on:
  push:
    tags:
      - 'contracts-v*.*.*'

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          version: 9.7.0

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Verify tag and packed consumer contract
        shell: bash
        run: |
          test "contracts-v$(node -p "require('./packages/contracts/package.json').version")" = "$GITHUB_REF_NAME"
          pnpm --filter @globiguard/contracts typecheck
          pnpm --filter @globiguard/contracts test
          pnpm --filter @globiguard/contracts build
          pnpm --filter @globiguard/sdk build
          node scripts/verify-packed-sdk-consumer.mjs

      - name: Publish with npm provenance
        run: pnpm --filter @globiguard/contracts publish --no-git-checks --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
