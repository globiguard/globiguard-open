# Changelog

## 1.0.2

- Publish the current governed-action context fields already used by the SDK,
  including correlation/workflow identifiers, policy, connector binding, actor,
  purpose, and bounded metadata.
- Preserve the existing API while correcting the public type artifact consumed
  by fresh `@globiguard/sdk` installations.

## 1.0.1

- Initial public shared-contract package.
